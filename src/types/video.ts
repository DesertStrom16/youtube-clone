export type Video = {
  title: string;
  channel: string;
  viewCount: string;
  uploadDate: string;
  desc: string;
  videoId: string;
  thumbnailUrl: string;
  avatarUrl: string;
  length: string;
};
